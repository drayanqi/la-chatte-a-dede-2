---
updated: 2026-09-12T22:43
---

- (dynamic) Pelo shipped story 3.1 (tactics API CRUD, store wired but unconsumed) and opened the room on a new want: unlimited tactics as tabs above the field canvas, with a '+' button. Room's tension: this overlaps/pivots story 3.2's planned LineupDialog, and dirty-state-on-tab-switch is the raw nerve.
- (outcome) Decisions locked by Pelo: tabs REPLACE story 3.2's LineupDialog (tabs = the lineup manager); '+' POSTs a default-formation lineup instantly; NO draft state — every tactic edit auto-saves on action completion (drop/rename), only Monaco keeps draft state; tabs renamable (double-click). Open threads: delete affordance + delete-active-tab behavior, tab overflow, system tactics, updating story 3.2 file.
- (outcome by bmad-agent-pm) Story 3.2 rewritten on disk (_bmad-output/implementation-artifacts/3-2-team-lineup-configuration-ui.md): TabBar replaces LineupDialog, 7 ACs, 8 tasks, new engine getTactic() read-back required (none existed), default formation = demo 5v5 slots, epics.md flagged as needing matching update. Status stays ready-for-dev.
- (dynamic) Story 3.2 implemented uncommitted (TabBar/auto-save/getTactic/bridging, 296 unit green, tabs e2e 9/9, lint+typecheck clean; full workspace+auth e2e verification still pending). Pelo's new thread: the demo-tactic fallback is incoherent — field must ALWAYS be backed by a real tactic; wants a default tactic when none exists. Room options: A) auto-create 'Tactic 1' on first load, B) backend system tactic (schema has nullable user_id + is_system), C) system default + copy-on-write assignment.
- (outcome by bmad-agent-dev) Default-tactic thread resolved: Pelo chose option A (frontend auto-create) then extended it — deleting the LAST tactic recreates a default one (always-≥1 invariant lives in tacticsStore.deleteTactic, delete affordance now always visible on active tab). Demo tactic deleted from AppShell; canvas always backed by a real tactic. Story 3.2 file amended (AC #5/#6, tasks checked, Dev Agent Record filled). Verification: unit 297/297, lint 0 errors, tsc clean, tactic-tabs e2e 9/9, full workspace+auth e2e 187 passed / 0 failed (3 Monaco-flaky on first try, pre-existing). 3.2 still uncommitted — ready for Pelo's walkthrough/commit.
