/**
 * WorkspacePage - Main workspace for authenticated users
 * OWNER: Dev Team
 */

import { AppShell } from '@/components/layout';

export const WorkspacePage: React.FC = () => {
  return <AppShell />;
};
