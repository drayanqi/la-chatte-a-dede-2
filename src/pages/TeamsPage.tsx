/**
 * TeamsPage - The atelier (story 7.2): team tabs, scripts, code, terrain.
 * The La Ronde Scripts | Code | Terrain layout lands in story 7.5; until
 * then the proven AppShell workspace hosts the route.
 */

import { AppShell } from '@/components/layout/AppShell';

export const TeamsPage: React.FC = () => {
  return <AppShell />;
};
